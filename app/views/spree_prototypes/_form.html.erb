<%= form_for(@spree_prototype) do |f| %>
  <% if @spree_prototype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spree_prototype.errors.count, "error") %> prohibited this spree_prototype from being saved:</h2>

      <ul>
      <% @spree_prototype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
